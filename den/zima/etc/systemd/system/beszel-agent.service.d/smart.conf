[Service]
# The packaged unit sets DeviceAllow=char-rtc r. A non-empty DeviceAllow list
# flips DevicePolicy=auto from "allow everything" to "allow only these", which
# blocks smartctl from opening /dev/sd* (exit status 2).
DeviceAllow=block-sd r
