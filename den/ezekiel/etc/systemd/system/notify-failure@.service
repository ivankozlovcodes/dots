[Unit]
Description=Telegram notification that %i failed
# Instance name is the failed unit, passed in via OnFailure=notify-failure@
%n.service

[Service]
Type=oneshot
EnvironmentFile=/etc/notify-failure.env
ExecStart=/usr/bin/curl -sS --max-time 20 \
  -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
  -d chat_id=${TG_CHAT} \
  --data-urlencode "text=🔴 %H: %i entered failed state"
